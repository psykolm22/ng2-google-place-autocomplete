machine:
  node:
    version: 6

dependencies:
  pre:
    - curl -o- -L https://yarnpkg.com/install.sh | bash
    - yarn global add protractor coveralls
    - yarn version
  override:
    - yarn install

test:
  override:
    - echo "test"

    